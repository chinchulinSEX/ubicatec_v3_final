name: ubicatec_unificado
description: UBICATEC unificado con AR funcional
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🌍 Mapas y ubicación
  google_maps_flutter: ^2.8.0
  mapbox_maps_flutter: ^2.11.0
  geolocator: 9.0.2
  location: ^7.0.0

  # 📸 Cámara y sensores
  camera: ^0.11.0+2
  sensors_plus: ^5.0.1
  flutter_compass: ^0.8.1
  permission_handler: ^10.1.0  

  # 🚀 Realidad Aumentada (RESTAURADO)
  ar_flutter_plugin:
    git:
      url: https://github.com/CariusLars/ar_flutter_plugin.git
      ref: main
  vector_math: ^2.1.4
  audioplayers: ^5.2.1

  # 🧰 Utilidades
  flutter_dotenv: ^6.0.0
  sliding_up_panel2: ^3.3.0+1
  phone_numbers_parser: ^9.0.15
  go_router: ^14.2.7
  http: ^1.5.0
  shared_preferences: ^2.5.3
  package_info_plus: ^8.3.1
  url_launcher: ^6.3.2

# ✅ ELIMINADO dependency_overrides que causaba conflictos

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  mockito: ^5.5.1
  build_runner: ^2.9.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/icons/
    - .env